<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_recortado"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_recortado"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlantasUrbanas"
        android:hardwareAccelerated="true"
        tools:targetApi="31">
        <activity
            android:name=".StockActivity"
            android:exported="false" />
        <activity
            android:name=".MovimientoActivity"
            android:exported="false" />
        <activity
            android:name=".NuevoProductoActivity"
            android:exported="false" />
        <activity
            android:name=".HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="portrait"
            tools:replace="screenOrientation">
        </activity>
    </application>

</manifest>